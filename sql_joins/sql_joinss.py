# # INNER JOIN (find common from Player and Batting tables)
# # SELllayer INNER JOIN batting where player.pid = batting.pid;
# # select player, count(*) from player group by country; 
# # CREATE VIEW all_batsman AS SELECT pid,norder.nout, from batting;
# # select mid from batsamn inner join pid,name from players 
# create view m_2869 as select mid from batting where mid=2689